name: E2E Tests

on:
  pull_request:
    branches: [ "main" ]

jobs:
  playwright-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "E2E tests require a deployed environment and are deferred to the CD pipeline."
